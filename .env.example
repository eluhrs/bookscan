POSTGRES_USER=bookscan
POSTGRES_PASSWORD=changeme
POSTGRES_DB=bookscan

# Generate with: openssl rand -hex 32
SECRET_KEY=change-this-to-a-random-secret

APP_USERNAME=admin

# Generate with: docker compose run --rm api python -c \
#   "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt']).hash('yourpassword'))"
PASSWORD_HASH=$2b$12$...
