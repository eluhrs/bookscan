# CHANGES-07 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wireframe alignment pass for all three workflow screens plus a new `needs_photo_review` database field supporting a "Review Photography?" checkbox on the Review screen.

**Architecture:** Global changes go into `WorkflowWrapper` first (step indicator/button bar backgrounds, "Start Over" rename, shared button height, optional `viewportHeight` prop for keyboard layout). Per-screen changes follow in dependency order: PhotographStep → PhotoWorkflowPage → LookupStep → ReviewStep. Backend migration and schema changes are self-contained and can go first.

**Tech Stack:** Python 3.12 / FastAPI / SQLAlchemy / Alembic (backend); React 18 / TypeScript / Vite / Vitest (frontend); `lucide-react` (new dependency for Camera icon).

---

## File Map

| File | Action | What changes |
|---|---|---|
| `api/alembic/versions/004_add_needs_photo_review.py` | **Create** | Migration: add `needs_photo_review` boolean column |
| `api/app/models.py` | **Modify** | Add `needs_photo_review` mapped column |
| `api/app/schemas.py` | **Modify** | Add `needs_photo_review` to `BookCreate`, `BookUpdate`, `BookResponse` |
| `api/app/routers/books.py` | **Modify** | Pass `needs_photo_review` in `create_book` and `list_books` manual `BookResponse` construction |
| `api/tests/test_books.py` | **Modify** | Tests for `needs_photo_review` field |
| `frontend/package.json` | **Modify** | Add `lucide-react` dependency |
| `frontend/src/types.ts` | **Modify** | Add `needs_photo_review: boolean` to `Book` interface |
| `frontend/src/components/workflow/WorkflowWrapper.tsx` | **Modify** | FEAT-02/03/04, `minHeight: 56px`, optional `viewportHeight` prop |
| `frontend/src/components/workflow/PhotographStep.tsx` | **Modify** | FEAT-05/06/07, BUG-01/02, `onSkip` prop |
| `frontend/src/pages/PhotoWorkflowPage.tsx` | **Modify** | `skippedPhotography` state, `handleSkip`, pass to ReviewStep |
| `frontend/src/components/workflow/LookupStep.tsx` | **Modify** | BUG-04/05/06/07/08/09/10/11/12, FEAT-08/09 |
| `frontend/src/components/workflow/ReviewStep.tsx` | **Modify** | Label renames, new checkbox, `skippedPhotography` prop, `needs_photo_review` in save |
| `CLAUDE.md` | **Modify** | Design Conventions section, CHANGES-07 completion entry |

---

## Task 1: Backend — migration, model, schemas, router

**Files:**
- Create: `api/alembic/versions/004_add_needs_photo_review.py`
- Modify: `api/app/models.py`
- Modify: `api/app/schemas.py`
- Modify: `api/app/routers/books.py`
- Modify: `api/tests/test_books.py`

- [ ] **Step 1: Write failing tests**

Append to `api/tests/test_books.py`:

```python
@pytest.mark.asyncio
async def test_create_book_needs_photo_review_default(client, auth_headers):
    resp = await client.post(
        "/api/books", json={"isbn": "9780001000001"}, headers=auth_headers
    )
    assert resp.status_code == 201
    assert resp.json()["needs_photo_review"] is False


@pytest.mark.asyncio
async def test_create_book_needs_photo_review_true(client, auth_headers):
    resp = await client.post(
        "/api/books",
        json={"isbn": "9780001000002", "needs_photo_review": True},
        headers=auth_headers,
    )
    assert resp.status_code == 201
    assert resp.json()["needs_photo_review"] is True


@pytest.mark.asyncio
async def test_update_book_needs_photo_review(client, auth_headers):
    create = await client.post(
        "/api/books", json={"isbn": "9780001000003"}, headers=auth_headers
    )
    book_id = create.json()["id"]
    resp = await client.patch(
        f"/api/books/{book_id}",
        json={"needs_photo_review": True},
        headers=auth_headers,
    )
    assert resp.status_code == 200
    assert resp.json()["needs_photo_review"] is True
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd api && .venv/bin/pytest tests/test_books.py::test_create_book_needs_photo_review_default tests/test_books.py::test_create_book_needs_photo_review_true tests/test_books.py::test_update_book_needs_photo_review -v
```

Expected: FAIL — `KeyError: 'needs_photo_review'` or similar.

- [ ] **Step 3: Create migration 004**

Create `api/alembic/versions/004_add_needs_photo_review.py`:

```python
"""add needs_photo_review to books

Revision ID: 004
Revises: 003
Create Date: 2026-04-08
"""
from alembic import op
import sqlalchemy as sa

revision = "004"
down_revision = "003"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "books",
        sa.Column(
            "needs_photo_review",
            sa.Boolean(),
            nullable=False,
            server_default=sa.false(),
        ),
    )


def downgrade() -> None:
    op.drop_column("books", "needs_photo_review")
```

- [ ] **Step 4: Add column to ORM model**

In `api/app/models.py`, add after the `condition` line (line 30):

```python
    needs_photo_review: Mapped[bool] = mapped_column(Boolean, default=False)
```

- [ ] **Step 5: Add field to schemas**

In `api/app/schemas.py`:

Add to `BookCreate` (after `condition: Optional[str] = None`):
```python
    needs_photo_review: bool = False
```

Add to `BookUpdate` (after `condition: Optional[str] = None`):
```python
    needs_photo_review: Optional[bool] = None
```

Add to `BookResponse` (after `condition: Optional[str] = None`):
```python
    needs_photo_review: bool = False
```

- [ ] **Step 6: Update manual BookResponse constructions in router**

`api/app/routers/books.py` — `create_book` handler (around line 96): add `needs_photo_review=book.needs_photo_review` before `created_at`:

```python
    return BookResponse(
        id=book.id,
        isbn=book.isbn,
        title=book.title,
        author=book.author,
        publisher=book.publisher,
        edition=book.edition,
        year=book.year,
        pages=book.pages,
        dimensions=book.dimensions,
        weight=book.weight,
        subject=book.subject,
        description=book.description,
        cover_image_url=book.cover_image_url,
        cover_image_local=book.cover_image_local,
        data_sources=book.data_sources,
        data_complete=book.data_complete,
        condition=book.condition,
        needs_photo_review=book.needs_photo_review,
        has_photos=False,
        photos=[],
        created_at=book.created_at,
        updated_at=book.updated_at,
    )
```

In `list_books` (the `BookResponse(...)` inside the list comprehension, around line 147–172): add `needs_photo_review=b.needs_photo_review` after `condition=b.condition`:

```python
        BookResponse(
            id=b.id,
            isbn=b.isbn,
            title=b.title,
            author=b.author,
            publisher=b.publisher,
            edition=b.edition,
            year=b.year,
            pages=b.pages,
            dimensions=b.dimensions,
            weight=b.weight,
            subject=b.subject,
            description=b.description,
            cover_image_url=b.cover_image_url,
            cover_image_local=b.cover_image_local,
            data_sources=b.data_sources,
            data_complete=b.data_complete,
            condition=b.condition,
            needs_photo_review=b.needs_photo_review,
            has_photos=b.id in photo_book_ids,
            photos=[],
            created_at=b.created_at,
            updated_at=b.updated_at,
        )
```

Note: `get_book` and `update_book` use `BookResponse.model_validate(book)` with `from_attributes=True` — they pick up `needs_photo_review` automatically once it's in the schema.

- [ ] **Step 7: Run all backend tests**

```bash
cd api && .venv/bin/pytest -v
```

Expected: All tests pass including the three new ones.

- [ ] **Step 8: Commit**

```bash
git add api/alembic/versions/004_add_needs_photo_review.py api/app/models.py api/app/schemas.py api/app/routers/books.py api/tests/test_books.py
git commit -m "feat: add needs_photo_review field — migration 004, model, schemas, router"
```

---

## Task 2: Install lucide-react

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json` (auto-generated)

- [ ] **Step 1: Install the package**

```bash
cd frontend && npm install lucide-react
```

Expected: `lucide-react` appears in `package.json` dependencies.

- [ ] **Step 2: Verify import works**

```bash
cd frontend && node -e "import('lucide-react').then(m => console.log('ok', Object.keys(m).includes('Camera')))"
```

Expected: `ok true`

- [ ] **Step 3: Commit**

```bash
git add frontend/package.json frontend/package-lock.json
git commit -m "chore: add lucide-react for Camera icon"
```

---

## Task 3: Frontend types — add needs_photo_review to Book

**Files:**
- Modify: `frontend/src/types.ts`

- [ ] **Step 1: Add field to Book interface**

In `frontend/src/types.ts`, add `needs_photo_review: boolean` after `has_photos: boolean` (line 19):

```typescript
export interface Book {
  id: string
  isbn: string
  title: string | null
  author: string | null
  publisher: string | null
  edition: string | null
  year: number | null
  pages: number | null
  dimensions: string | null
  weight: string | null
  subject: string | null
  description: string | null
  condition: string | null
  cover_image_url: string | null
  cover_image_local: string | null
  data_sources: Record<string, string> | null
  data_complete: boolean
  has_photos: boolean
  needs_photo_review: boolean
  created_at: string
  updated_at: string
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
cd frontend && npx tsc --noEmit
```

Expected: No errors. (If there are errors about `needs_photo_review` being required somewhere it's not provided, those will surface in later tasks.)

- [ ] **Step 3: Commit**

```bash
git add frontend/src/types.ts
git commit -m "feat: add needs_photo_review to Book type"
```

---

## Task 4: WorkflowWrapper — global design changes

**Files:**
- Modify: `frontend/src/components/workflow/WorkflowWrapper.tsx`

Changes: FEAT-02 (`#F0F0F0` step indicator bg), FEAT-03 (`#F0F0F0` secondary bar bg), FEAT-04 ("Cancel" → "Start Over"), shared `minHeight: 56px` on primary button, optional `viewportHeight` prop for keyboard layout.

- [ ] **Step 1: Rewrite WorkflowWrapper**

Replace the full contents of `frontend/src/components/workflow/WorkflowWrapper.tsx`:

```tsx
// frontend/src/components/workflow/WorkflowWrapper.tsx

import { ReactNode } from 'react'
import { Link } from 'react-router-dom'
import { theme } from '../../styles/theme'

type WorkflowStep = 'photograph' | 'lookup' | 'review'

const STEPS: WorkflowStep[] = ['photograph', 'lookup', 'review']
const STEP_LABELS: Record<WorkflowStep, string> = {
  photograph: 'Photograph',
  lookup: 'Metadata',
  review: 'Review',
}

export interface WorkflowWrapperProps {
  step: WorkflowStep
  controls: ReactNode
  hintText?: string
  primaryLabel: string
  primaryDisabled?: boolean
  onPrimary: () => void
  onCancel: () => void
  /** When provided, overrides 100dvh — used by LookupStep keyboard mode to track visualViewport height */
  viewportHeight?: number
}

export default function WorkflowWrapper({
  step,
  controls,
  hintText,
  primaryLabel,
  primaryDisabled = false,
  onPrimary,
  onCancel,
  viewportHeight,
}: WorkflowWrapperProps) {
  return (
    <div
      style={{
        height: viewportHeight ? `${viewportHeight}px` : '100dvh',
        background: theme.colors.surface,
        color: theme.colors.text,
        display: 'flex',
        flexDirection: 'column',
        overflow: 'hidden',
        fontFamily: theme.font.sans,
      }}
    >
      {/* Zone 1: Step indicator — #F0F0F0 background (FEAT-02) */}
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: '0.5rem',
          padding: '0.75rem 1rem 0.25rem',
          flexShrink: 0,
          background: '#F0F0F0',
        }}
      >
        {STEPS.map((s, i) => (
          <span
            key={s}
            style={{
              fontSize: '0.78rem',
              color: s === step ? theme.colors.text : theme.colors.muted,
              display: 'flex',
              alignItems: 'center',
              gap: '0.25rem',
            }}
          >
            {i > 0 && (
              <span style={{ color: theme.colors.muted, marginRight: '0.25rem' }}>·</span>
            )}
            <span style={{ fontSize: '0.6rem' }}>{s === step ? '●' : '○'}</span>
            <span>{STEP_LABELS[s]}</span>
          </span>
        ))}
      </div>

      {/* Zone 2: Controls bar */}
      <div
        style={{
          padding: '0.4rem 1rem',
          minHeight: '2.75rem',
          display: 'flex',
          alignItems: 'center',
          flexShrink: 0,
        }}
      >
        {controls}
      </div>

      {/* Zone 3: Main content — grows to fill remaining space */}
      <div style={{ flex: 1, overflow: 'hidden', position: 'relative' }}>
        {children}
      </div>

      {/* Zone 4: Hint text */}
      {hintText !== undefined && (
        <div
          style={{
            padding: '0.5rem 1.5rem',
            textAlign: 'center',
            flexShrink: 0,
          }}
        >
          <p style={{ margin: 0, fontSize: '0.82rem', color: theme.colors.muted }}>
            {hintText}
          </p>
        </div>
      )}

      {/* Zone 5: Primary button — shared minHeight 56px (BUG-03/06/12) */}
      <div style={{ padding: '0.5rem 1rem 0.25rem', flexShrink: 0 }}>
        <button
          onClick={onPrimary}
          disabled={primaryDisabled}
          style={{
            display: 'block',
            width: '100%',
            minHeight: 56,
            padding: '0.75rem 1rem',
            fontSize: '1.1rem',
            fontWeight: 700,
            letterSpacing: '0.06em',
            background: primaryDisabled ? theme.colors.disabled : theme.colors.accent,
            color: primaryDisabled ? theme.colors.disabledText : '#fff',
            border: 'none',
            borderRadius: 12,
            cursor: primaryDisabled ? 'default' : 'pointer',
            fontFamily: theme.font.sans,
          }}
        >
          {primaryLabel}
        </button>
      </div>

      {/* Zone 6: Secondary buttons — #F0F0F0 background (FEAT-03), "Start Over" label (FEAT-04) */}
      <div
        style={{
          display: 'flex',
          gap: '0.5rem',
          padding: '0.25rem 1rem 1rem',
          flexShrink: 0,
          background: '#F0F0F0',
        }}
      >
        <Link
          to="/dashboard"
          style={{
            flex: 1,
            display: 'block',
            textAlign: 'center',
            padding: '0.6rem',
            background: theme.colors.subtle,
            color: theme.colors.subtleText,
            borderRadius: 8,
            textDecoration: 'none',
            fontSize: '0.875rem',
            fontFamily: theme.font.sans,
          }}
        >
          Dashboard
        </Link>
        <button
          onClick={onCancel}
          style={{
            flex: 1,
            padding: '0.6rem',
            background: theme.colors.subtle,
            color: theme.colors.subtleText,
            border: 'none',
            borderRadius: 8,
            cursor: 'pointer',
            fontSize: '0.875rem',
            fontFamily: theme.font.sans,
          }}
        >
          Start Over
        </button>
      </div>
    </div>
  )
}
```

Wait — `children` is missing from the destructured props. Fix: add `children: ReactNode` to `WorkflowWrapperProps` and to the destructure. The full correct file:

```tsx
// frontend/src/components/workflow/WorkflowWrapper.tsx

import { ReactNode } from 'react'
import { Link } from 'react-router-dom'
import { theme } from '../../styles/theme'

type WorkflowStep = 'photograph' | 'lookup' | 'review'

const STEPS: WorkflowStep[] = ['photograph', 'lookup', 'review']
const STEP_LABELS: Record<WorkflowStep, string> = {
  photograph: 'Photograph',
  lookup: 'Metadata',
  review: 'Review',
}

export interface WorkflowWrapperProps {
  step: WorkflowStep
  controls: ReactNode
  hintText?: string
  primaryLabel: string
  primaryDisabled?: boolean
  onPrimary: () => void
  onCancel: () => void
  children: ReactNode
  /** Overrides 100dvh — used by LookupStep keyboard mode to track visualViewport height */
  viewportHeight?: number
}

export default function WorkflowWrapper({
  step,
  controls,
  hintText,
  primaryLabel,
  primaryDisabled = false,
  onPrimary,
  onCancel,
  children,
  viewportHeight,
}: WorkflowWrapperProps) {
  return (
    <div
      style={{
        height: viewportHeight ? `${viewportHeight}px` : '100dvh',
        background: theme.colors.surface,
        color: theme.colors.text,
        display: 'flex',
        flexDirection: 'column',
        overflow: 'hidden',
        fontFamily: theme.font.sans,
      }}
    >
      {/* Zone 1: Step indicator — #F0F0F0 background (FEAT-02) */}
      <div
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: '0.5rem',
          padding: '0.75rem 1rem 0.25rem',
          flexShrink: 0,
          background: '#F0F0F0',
        }}
      >
        {STEPS.map((s, i) => (
          <span
            key={s}
            style={{
              fontSize: '0.78rem',
              color: s === step ? theme.colors.text : theme.colors.muted,
              display: 'flex',
              alignItems: 'center',
              gap: '0.25rem',
            }}
          >
            {i > 0 && (
              <span style={{ color: theme.colors.muted, marginRight: '0.25rem' }}>·</span>
            )}
            <span style={{ fontSize: '0.6rem' }}>{s === step ? '●' : '○'}</span>
            <span>{STEP_LABELS[s]}</span>
          </span>
        ))}
      </div>

      {/* Zone 2: Controls bar */}
      <div
        style={{
          padding: '0.4rem 1rem',
          minHeight: '2.75rem',
          display: 'flex',
          alignItems: 'center',
          flexShrink: 0,
        }}
      >
        {controls}
      </div>

      {/* Zone 3: Main content */}
      <div style={{ flex: 1, overflow: 'hidden', position: 'relative' }}>
        {children}
      </div>

      {/* Zone 4: Hint text (errors or keyboard instructions) */}
      {hintText !== undefined && (
        <div
          style={{
            padding: '0.5rem 1.5rem',
            textAlign: 'center',
            flexShrink: 0,
          }}
        >
          <p style={{ margin: 0, fontSize: '0.82rem', color: theme.colors.muted }}>
            {hintText}
          </p>
        </div>
      )}

      {/* Zone 5: Primary button — shared minHeight 56px */}
      <div style={{ padding: '0.5rem 1rem 0.25rem', flexShrink: 0 }}>
        <button
          onClick={onPrimary}
          disabled={primaryDisabled}
          style={{
            display: 'block',
            width: '100%',
            minHeight: 56,
            padding: '0.75rem 1rem',
            fontSize: '1.1rem',
            fontWeight: 700,
            letterSpacing: '0.06em',
            background: primaryDisabled ? theme.colors.disabled : theme.colors.accent,
            color: primaryDisabled ? theme.colors.disabledText : '#fff',
            border: 'none',
            borderRadius: 12,
            cursor: primaryDisabled ? 'default' : 'pointer',
            fontFamily: theme.font.sans,
          }}
        >
          {primaryLabel}
        </button>
      </div>

      {/* Zone 6: Secondary buttons — #F0F0F0 background, "Start Over" label */}
      <div
        style={{
          display: 'flex',
          gap: '0.5rem',
          padding: '0.25rem 1rem 1rem',
          flexShrink: 0,
          background: '#F0F0F0',
        }}
      >
        <Link
          to="/dashboard"
          style={{
            flex: 1,
            display: 'block',
            textAlign: 'center',
            padding: '0.6rem',
            background: theme.colors.subtle,
            color: theme.colors.subtleText,
            borderRadius: 8,
            textDecoration: 'none',
            fontSize: '0.875rem',
            fontFamily: theme.font.sans,
          }}
        >
          Dashboard
        </Link>
        <button
          onClick={onCancel}
          style={{
            flex: 1,
            padding: '0.6rem',
            background: theme.colors.subtle,
            color: theme.colors.subtleText,
            border: 'none',
            borderRadius: 8,
            cursor: 'pointer',
            fontSize: '0.875rem',
            fontFamily: theme.font.sans,
          }}
        >
          Start Over
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verify TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: No errors.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/workflow/WorkflowWrapper.tsx
git commit -m "feat: WorkflowWrapper — F0F0F0 zones, Start Over, 56px button, viewportHeight prop"
```

---

## Task 5: PhotographStep — controls bar, dropdown 0-5, SKIP

**Files:**
- Modify: `frontend/src/components/workflow/PhotographStep.tsx`

Changes: FEAT-05 (controls bar border + subtle fills), FEAT-06 (dropdown 0–5, SKIP label), FEAT-07 (`onSkip` prop, no photos on SKIP).

- [ ] **Step 1: Update PhotographStep interface and controls**

Replace the full contents of `frontend/src/components/workflow/PhotographStep.tsx`:

```tsx
// frontend/src/components/workflow/PhotographStep.tsx

import { useRef } from 'react'
import WorkflowWrapper from './WorkflowWrapper'
import { useCameraStream } from '../../hooks/useCameraStream'
import { useScanAudio } from '../../hooks/useScanAudio'
import { theme } from '../../styles/theme'

interface PhotographStepProps {
  photos: File[]
  targetCount: number
  onTargetCountChange: (n: number) => void
  onPhotoAdded: (file: File) => void
  onSkip: () => void
  onCancel: () => void
}

const PHOTO_HINTS = [
  'Position front cover',
  'Position back cover',
  'Position spine',
  'Position additional view',
  'Position additional view',
]

async function captureAndCompress(video: HTMLVideoElement): Promise<File> {
  return new Promise<File>((resolve, reject) => {
    const maxEdge = 1200
    let w = video.videoWidth
    let h = video.videoHeight
    if (w > maxEdge || h > maxEdge) {
      if (w > h) { h = Math.round((h * maxEdge) / w); w = maxEdge }
      else { w = Math.round((w * maxEdge) / h); h = maxEdge }
    }
    const canvas = document.createElement('canvas')
    canvas.width = w
    canvas.height = h
    canvas.getContext('2d')!.drawImage(video, 0, 0, w, h)
    canvas.toBlob(
      (blob) => {
        if (!blob) { reject(new Error('Frame capture failed')); return }
        resolve(new File([blob], `photo-${Date.now()}.jpg`, { type: 'image/jpeg' }))
      },
      'image/jpeg',
      0.85,
    )
  })
}

export default function PhotographStep({
  photos,
  targetCount,
  onTargetCountChange,
  onPhotoAdded,
  onSkip,
  onCancel,
}: PhotographStepProps) {
  const { videoRef, torchAvailable, torchOn, cameraError, handleTorchToggle } =
    useCameraStream({ enabled: true })
  const { playSuccess } = useScanAudio()
  const capturingRef = useRef(false)

  async function handleCapture() {
    if (capturingRef.current || !videoRef.current) return
    if (videoRef.current.videoWidth === 0) return
    capturingRef.current = true
    try {
      const file = await captureAndCompress(videoRef.current)
      playSuccess()
      onPhotoAdded(file)
    } finally {
      capturingRef.current = false
    }
  }

  const isSkipMode = targetCount === 0

  const hintIdx = Math.min(photos.length, PHOTO_HINTS.length - 1)
  const overlayHint = isSkipMode
    ? 'Set number of images, position book, then Capture'
    : PHOTO_HINTS[hintIdx]

  // □/■ progress indicators — hidden when targetCount === 0 (FEAT-06)
  const progressIndicators = isSkipMode ? null : Array.from({ length: targetCount }, (_, i) => (
    <span
      key={i}
      style={{
        fontSize: '1rem',
        color: i < photos.length ? theme.colors.text : theme.colors.muted,
        lineHeight: 1,
      }}
    >
      {i < photos.length ? '■' : '□'}
    </span>
  ))

  const controls = (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        width: '100%',
        border: '1px solid #E5E5E5',
        borderRadius: 8,
        padding: '0.2rem 0.5rem',
      }}
    >
      {/* Left: photo count dropdown (FEAT-05: subtle background) */}
      <label
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: '0.3rem',
          fontSize: '0.85rem',
          color: theme.colors.muted,
          cursor: 'pointer',
        }}
      >
        <span style={{ fontWeight: 600, fontSize: '0.9rem' }}>#</span>
        <select
          value={targetCount}
          onChange={(e) => onTargetCountChange(Number(e.target.value))}
          style={{
            background: theme.colors.subtle,
            color: theme.colors.text,
            border: `1px solid ${theme.colors.border}`,
            borderRadius: 6,
            padding: '0.2rem 0.4rem',
            fontSize: '0.85rem',
            fontFamily: theme.font.sans,
          }}
        >
          {[0, 1, 2, 3, 4, 5].map((n) => (
            <option key={n} value={n}>{n}</option>
          ))}
        </select>
      </label>

      {/* Center: □/■ progress (hidden when targetCount === 0) */}
      <div style={{ display: 'flex', gap: '0.4rem', alignItems: 'center' }}>
        {progressIndicators}
      </div>

      {/* Right: torch toggle (FEAT-05: already has subtle background) */}
      {torchAvailable ? (
        <button
          onClick={handleTorchToggle}
          style={{
            padding: '0.35rem 0.5rem',
            fontSize: '1.1rem',
            lineHeight: 1,
            background: torchOn ? '#FEF08A' : theme.colors.subtle,
            border: `1px solid ${theme.colors.border}`,
            borderRadius: 8,
            cursor: 'pointer',
          }}
          aria-label={torchOn ? 'Turn off torch' : 'Turn on torch'}
        >
          🔦
        </button>
      ) : (
        <div style={{ width: '2.25rem' }} />
      )}
    </div>
  )

  const cameraContent = cameraError ? (
    <div
      style={{
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '2rem',
      }}
    >
      <p style={{ color: theme.colors.danger, textAlign: 'center', fontSize: '0.9rem', margin: 0 }}>
        {cameraError}
      </p>
    </div>
  ) : (
    <div
      style={{
        height: '100%',
        padding: '0.5rem 1rem',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <div
        style={{
          position: 'relative',
          width: '100%',
          height: '100%',
          borderRadius: 12,
          overflow: 'hidden',
          border: `1px solid ${theme.colors.border}`,
          background: '#000',
        }}
      >
        <video
          ref={videoRef}
          style={{ width: '100%', height: '100%', objectFit: 'cover', display: 'block' }}
          autoPlay
          muted
          playsInline
        />

        {/* Square mask — BUG-01: implemented in Task 6 */}
        <div style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}>
          <div
            style={{
              position: 'absolute',
              left: '15%',
              right: '15%',
              top: '5%',
              bottom: '5%',
              boxShadow: '0 0 0 9999px rgba(0,0,0,0.45)',
              borderRadius: 4,
            }}
          >
            <div style={{ position: 'absolute', top: -2, left: -2, width: 22, height: 22, borderTop: `3px solid ${theme.colors.accent}`, borderLeft: `3px solid ${theme.colors.accent}`, borderRadius: '2px 0 0 0' }} />
            <div style={{ position: 'absolute', top: -2, right: -2, width: 22, height: 22, borderTop: `3px solid ${theme.colors.accent}`, borderRight: `3px solid ${theme.colors.accent}`, borderRadius: '0 2px 0 0' }} />
            <div style={{ position: 'absolute', bottom: -2, left: -2, width: 22, height: 22, borderBottom: `3px solid ${theme.colors.accent}`, borderLeft: `3px solid ${theme.colors.accent}`, borderRadius: '0 0 0 2px' }} />
            <div style={{ position: 'absolute', bottom: -2, right: -2, width: 22, height: 22, borderBottom: `3px solid ${theme.colors.accent}`, borderRight: `3px solid ${theme.colors.accent}`, borderRadius: '0 0 2px 0' }} />

            {/* Hint text inside mask — BUG-02 */}
            <div style={{ position: 'absolute', bottom: 12, left: 8, right: 8, textAlign: 'center' }}>
              <span
                style={{
                  display: 'inline-block',
                  background: 'rgba(0,0,0,0.55)',
                  color: '#fff',
                  fontSize: '0.78rem',
                  padding: '0.25rem 0.75rem',
                  borderRadius: 20,
                }}
              >
                {overlayHint}
              </span>
            </div>
          </div>
        </div>
      </div>
    </div>
  )

  return (
    <WorkflowWrapper
      step="photograph"
      controls={controls}
      primaryLabel={isSkipMode ? 'SKIP' : 'CAPTURE'}
      onPrimary={isSkipMode ? onSkip : handleCapture}
      onCancel={onCancel}
    >
      {cameraContent}
    </WorkflowWrapper>
  )
}
```

Note: The square mask (BUG-01) is left as the existing rectangle placeholder in this task — it gets replaced in Task 6 with the ResizeObserver implementation. The `hintText` prop is no longer passed to WorkflowWrapper (BUG-02 done).

- [ ] **Step 2: Verify TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: Error about `onSkip` missing from `PhotographStep` usage in `PhotoWorkflowPage.tsx`. That's expected — Task 7 fixes it.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/workflow/PhotographStep.tsx
git commit -m "feat: PhotographStep — controls border, dropdown 0-5, SKIP mode, hint text fix"
```

---

## Task 6: PhotographStep — square mask (BUG-01)

**Files:**
- Modify: `frontend/src/components/workflow/PhotographStep.tsx`

- [ ] **Step 1: Add ResizeObserver for square mask**

The camera container div (the one with `position: relative`, `width: 100%`, `height: 100%`, `borderRadius: 12`) needs a ref. The mask div needs to use measured dimensions instead of percentage-based positioning.

Replace the camera content section of `PhotographStep.tsx`. Add `useRef`, `useState`, `useEffect` to imports, then update the camera view:

At the top of the file, update imports:
```tsx
import { useRef, useState, useEffect } from 'react'
```

Add these hooks inside the `PhotographStep` component (after the existing `capturingRef`):
```tsx
  const containerRef = useRef<HTMLDivElement>(null)
  const [squareSide, setSquareSide] = useState(0)

  useEffect(() => {
    const el = containerRef.current
    if (!el) return
    const ro = new ResizeObserver(([entry]) => {
      const { width, height } = entry.contentRect
      setSquareSide(Math.min(width, height) - 24)
    })
    ro.observe(el)
    return () => ro.disconnect()
  }, [])
```

Replace the camera container div and mask inside `cameraContent` (the non-error branch). The container gets `ref={containerRef}` and the mask uses `squareSide`:

```tsx
      <div
        ref={containerRef}
        style={{
          position: 'relative',
          width: '100%',
          height: '100%',
          borderRadius: 12,
          overflow: 'hidden',
          border: `1px solid ${theme.colors.border}`,
          background: '#000',
        }}
      >
        <video
          ref={videoRef}
          style={{ width: '100%', height: '100%', objectFit: 'cover', display: 'block' }}
          autoPlay
          muted
          playsInline
        />

        {/* Square mask overlay — BUG-01 */}
        {squareSide > 0 && (
          <div style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}>
            <div
              style={{
                position: 'absolute',
                width: squareSide,
                height: squareSide,
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                boxShadow: '0 0 0 9999px rgba(0,0,0,0.45)',
                borderRadius: 4,
              }}
            >
              <div style={{ position: 'absolute', top: -2, left: -2, width: 22, height: 22, borderTop: `3px solid ${theme.colors.accent}`, borderLeft: `3px solid ${theme.colors.accent}`, borderRadius: '2px 0 0 0' }} />
              <div style={{ position: 'absolute', top: -2, right: -2, width: 22, height: 22, borderTop: `3px solid ${theme.colors.accent}`, borderRight: `3px solid ${theme.colors.accent}`, borderRadius: '0 2px 0 0' }} />
              <div style={{ position: 'absolute', bottom: -2, left: -2, width: 22, height: 22, borderBottom: `3px solid ${theme.colors.accent}`, borderLeft: `3px solid ${theme.colors.accent}`, borderRadius: '0 0 0 2px' }} />
              <div style={{ position: 'absolute', bottom: -2, right: -2, width: 22, height: 22, borderBottom: `3px solid ${theme.colors.accent}`, borderRight: `3px solid ${theme.colors.accent}`, borderRadius: '0 0 2px 0' }} />

              {/* Hint text: "Set number of images, position book, then Capture" — BUG-02 */}
              <div style={{ position: 'absolute', bottom: 12, left: 8, right: 8, textAlign: 'center' }}>
                <span
                  style={{
                    display: 'inline-block',
                    background: 'rgba(0,0,0,0.55)',
                    color: '#fff',
                    fontSize: '0.78rem',
                    padding: '0.25rem 0.75rem',
                    borderRadius: 20,
                  }}
                >
                  Set number of images, position book, then Capture
                </span>
              </div>
            </div>
          </div>
        )}
      </div>
```

Remove the `overlayHint` variable and `hintIdx` computation — the hint text is now static in this mask.

- [ ] **Step 2: Verify TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: Same `onSkip` error from Task 5 still present (fixed in Task 7). No new errors.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/workflow/PhotographStep.tsx
git commit -m "feat: PhotographStep — square mask via ResizeObserver (BUG-01)"
```

---

## Task 7: PhotoWorkflowPage — SKIP state

**Files:**
- Modify: `frontend/src/pages/PhotoWorkflowPage.tsx`

- [ ] **Step 1: Add skippedPhotography state and handleSkip**

Replace the full contents of `frontend/src/pages/PhotoWorkflowPage.tsx`:

```tsx
import { useState, useCallback, useRef } from 'react'
import PhotographStep from '../components/workflow/PhotographStep'
import LookupStep from '../components/workflow/LookupStep'
import ReviewStep from '../components/workflow/ReviewStep'
import { useScanAudio } from '../hooks/useScanAudio'
import { BookLookup } from '../types'

type WorkflowStep = 'photograph' | 'lookup' | 'review'

export default function PhotoWorkflowPage() {
  const [step, setStep] = useState<WorkflowStep>('photograph')
  const [photos, setPhotos] = useState<File[]>([])
  const [targetCount, setTargetCount] = useState<number>(
    () => Number(localStorage.getItem('photoTargetCount') ?? 3)
  )
  const [lookupResult, setLookupResult] = useState<BookLookup | null>(null)
  const [savedBookId, setSavedBookId] = useState<string | null>(null)
  const [skippedPhotography, setSkippedPhotography] = useState(false)

  const stepRef = useRef<WorkflowStep>('photograph')

  const { playSuccess, playReview } = useScanAudio()

  function handlePhotoAdded(file: File) {
    setPhotos((prev) => {
      const next = [...prev, file]
      if (next.length >= targetCount) {
        stepRef.current = 'lookup'
        setStep('lookup')
      }
      return next
    })
  }

  function handleTargetCountChange(n: number) {
    setTargetCount(n)
    localStorage.setItem('photoTargetCount', String(n))
  }

  function handleSkip() {
    setSkippedPhotography(true)
    stepRef.current = 'lookup'
    setStep('lookup')
  }

  const handleLookupComplete = useCallback(
    (result: BookLookup) => {
      if (stepRef.current !== 'lookup') return
      setLookupResult(result)
      setSavedBookId(null)
      if (result.data_complete) playSuccess()
      else playReview()
      stepRef.current = 'review'
      setStep('review')
    },
    [playSuccess, playReview]
  )

  function handleCancel() {
    stepRef.current = 'photograph'
    setPhotos([])
    setLookupResult(null)
    setSavedBookId(null)
    setSkippedPhotography(false)
    setStep('photograph')
  }

  function handleSaveComplete() {
    stepRef.current = 'photograph'
    setPhotos([])
    setLookupResult(null)
    setSavedBookId(null)
    setSkippedPhotography(false)
    setStep('photograph')
  }

  if (step === 'photograph') {
    return (
      <PhotographStep
        photos={photos}
        targetCount={targetCount}
        onTargetCountChange={handleTargetCountChange}
        onPhotoAdded={handlePhotoAdded}
        onSkip={handleSkip}
        onCancel={handleCancel}
      />
    )
  }

  if (step === 'lookup') {
    return (
      <LookupStep
        onLookupComplete={handleLookupComplete}
        onCancel={handleCancel}
      />
    )
  }

  if (step === 'review' && lookupResult) {
    return (
      <ReviewStep
        lookupResult={lookupResult}
        photos={photos}
        savedBookId={savedBookId}
        onSavedBookId={setSavedBookId}
        onSaveComplete={handleSaveComplete}
        onCancel={handleCancel}
        skippedPhotography={skippedPhotography}
      />
    )
  }

  return null
}
```

- [ ] **Step 2: Verify TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: Error about `skippedPhotography` prop missing from `ReviewStep` — fixed in Task 10. No other errors.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/PhotoWorkflowPage.tsx
git commit -m "feat: PhotoWorkflowPage — skippedPhotography state and handleSkip"
```

---

## Task 8: LookupStep — camera mode (BUG-04, BUG-05, FEAT-08)

**Files:**
- Modify: `frontend/src/components/workflow/LookupStep.tsx`

Changes: 3:1 landscape mask (BUG-04), hint text inside mask only / errors to hintText (BUG-05), controls bar border + subtle fills (FEAT-08).

- [ ] **Step 1: Update camera mode in LookupStep**

The camera mode changes are isolated to: the mask dimensions, the hint text inside the mask, the `hintText` prop logic, and the controls bar styling. Apply these changes to `LookupStep.tsx`:

**Controls bar** — add `border: '1px solid #E5E5E5'` wrapper and `background: theme.colors.subtle` to keyboard button:

```tsx
  const cameraControls = (
    <div
      style={{
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
        width: '100%',
        border: '1px solid #E5E5E5',
        borderRadius: 8,
        padding: '0.2rem 0.5rem',
      }}
    >
      <button
        aria-label="Switch to keyboard input"
        onClick={() => { setMode('keyboard'); setHintError(null) }}
        style={{
          background: theme.colors.subtle,
          border: `1px solid ${theme.colors.border}`,
          color: theme.colors.text,
          cursor: 'pointer',
          fontSize: '1.1rem',
          padding: '0.3rem 0.5rem',
          borderRadius: 8,
          lineHeight: 1,
        }}
        title="Switch to keyboard"
      >
        ⌨
      </button>
      {torchAvailable && (
        <button
          onClick={handleTorchToggle}
          style={{
            padding: '0.35rem 0.5rem',
            fontSize: '1.1rem',
            lineHeight: 1,
            background: torchOn ? '#FEF08A' : theme.colors.subtle,
            border: `1px solid ${theme.colors.border}`,
            borderRadius: 8,
            cursor: 'pointer',
          }}
          aria-label={torchOn ? 'Turn off torch' : 'Turn on torch'}
        >
          🔦
        </button>
      )}
    </div>
  )
```

**Landscape mask** — change to ~3:1 ratio (`top: '32%', bottom: '32%'`):

```tsx
            <div
              style={{
                position: 'absolute',
                left: '5%',
                right: '5%',
                top: '32%',
                bottom: '32%',
                boxShadow: '0 0 0 9999px rgba(0,0,0,0.45)',
                borderRadius: 4,
              }}
            >
```

**Hint text inside mask** — change pill text from `'Align barcode within frame'` to `'Align barcode then tap Lookup, or use keyboard'`:

```tsx
              <div style={{ position: 'absolute', bottom: 10, left: 8, right: 8, textAlign: 'center' }}>
                <span
                  style={{
                    display: 'inline-block',
                    background: 'rgba(0,0,0,0.55)',
                    color: '#fff',
                    fontSize: '0.78rem',
                    padding: '0.25rem 0.75rem',
                    borderRadius: 20,
                  }}
                >
                  Align barcode then tap Lookup, or use keyboard
                </span>
              </div>
```

**hintText logic** — camera mode now only shows errors; keyboard mode shows errors only too (placeholder handles instruction). Replace the existing `hintText` computation:

```tsx
  const hintText = hintError ?? undefined
```

- [ ] **Step 2: Verify TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: Same `skippedPhotography` error from Task 7 still present. No new errors.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/workflow/LookupStep.tsx
git commit -m "feat: LookupStep camera — 3:1 mask, hint inside, controls bar border (BUG-04/05, FEAT-08)"
```

---

## Task 9: LookupStep — keyboard mode (BUG-07–12, FEAT-09)

**Files:**
- Modify: `frontend/src/components/workflow/LookupStep.tsx`

Changes: visualViewport keyboard detection (BUG-07/08/09), Lucide Camera icon (BUG-10), placeholder text (BUG-11), controls bar border (FEAT-09). LOOKUP button height already handled by WorkflowWrapper `minHeight: 56px` (BUG-12).

- [ ] **Step 1: Add visualViewport tracking and update keyboard mode**

Add `useEffect` to imports at the top of LookupStep.tsx:

```tsx
import { useRef, useState, useEffect } from 'react'
```

Add `Camera` to lucide-react import:

```tsx
import { Camera } from 'lucide-react'
```

Add `viewportHeight` state inside `LookupStep` (after the existing state declarations):

```tsx
  const [viewportHeight, setViewportHeight] = useState<number | undefined>(undefined)

  useEffect(() => {
    if (mode !== 'keyboard') {
      setViewportHeight(undefined)
      return
    }
    const vv = window.visualViewport
    if (!vv) return
    const handler = () => setViewportHeight(vv.height)
    vv.addEventListener('resize', handler)
    setViewportHeight(vv.height)
    return () => vv.removeEventListener('resize', handler)
  }, [mode])
```

Replace the `keyboardControls` variable:

```tsx
  const keyboardControls = (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        width: '100%',
        border: '1px solid #E5E5E5',
        borderRadius: 8,
        padding: '0.2rem 0.5rem',
      }}
    >
      <button
        aria-label="Switch to camera"
        onClick={() => { setMode('camera'); setHintError(null) }}
        style={{
          background: theme.colors.subtle,
          border: `1px solid ${theme.colors.border}`,
          color: theme.colors.text,
          cursor: 'pointer',
          padding: '0.3rem 0.5rem',
          borderRadius: 8,
          lineHeight: 1,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
        title="Switch to camera"
      >
        <Camera size={18} />
      </button>
    </div>
  )
```

Replace the keyboard `mainContent` branch. The key changes: remove the label, update placeholder text + inject `<style>` for darker placeholder color, and vertically center the input:

```tsx
  ) : (
    <div
      style={{
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '2rem',
      }}
    >
      <style>{`.isbn-input::placeholder { color: #6B7280; }`}</style>
      <input
        className="isbn-input"
        type="text"
        inputMode="numeric"
        placeholder="Type ISBN-10 or ISBN-13, then tap Lookup"
        value={isbn}
        onChange={(e) => setIsbn(e.target.value)}
        autoFocus
        style={{
          width: '100%',
          maxWidth: 320,
          padding: '0.75rem 1rem',
          fontSize: '1.1rem',
          background: '#fff',
          color: theme.colors.text,
          border: `1px solid ${theme.colors.border}`,
          borderRadius: 8,
          outline: 'none',
          textAlign: 'center',
          fontFamily: theme.font.sans,
        }}
      />
    </div>
  )
```

Pass `viewportHeight` to `WorkflowWrapper` in the return statement. Update the `return`:

```tsx
  return (
    <WorkflowWrapper
      step="lookup"
      controls={mode === 'camera' ? cameraControls : keyboardControls}
      hintText={hintText}
      primaryLabel={looking ? 'Looking up…' : 'LOOKUP'}
      primaryDisabled={looking}
      onPrimary={mode === 'camera' ? handleCameraLookup : handleKeyboardLookup}
      onCancel={onCancel}
      viewportHeight={mode === 'keyboard' ? viewportHeight : undefined}
    >
      {mainContent}
    </WorkflowWrapper>
  )
```

- [ ] **Step 2: Verify TypeScript**

```bash
cd frontend && npx tsc --noEmit
```

Expected: Same `skippedPhotography` error only. No new errors.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/workflow/LookupStep.tsx
git commit -m "feat: LookupStep keyboard — viewport detection, Camera icon, placeholder (BUG-07–12, FEAT-09)"
```

---

## Task 10: ReviewStep — label renames, new checkbox, save payload

**Files:**
- Modify: `frontend/src/components/workflow/ReviewStep.tsx`

Changes: "Mark for Review?" → "Review Metadata?", new "Review Photography?" checkbox auto-checked on `skippedPhotography`, `needs_photo_review` in save payload.

- [ ] **Step 1: Update ReviewStep**

Replace the full contents of `frontend/src/components/workflow/ReviewStep.tsx`:

```tsx
// frontend/src/components/workflow/ReviewStep.tsx

import { useState } from 'react'
import WorkflowWrapper from './WorkflowWrapper'
import { BookLookup } from '../../types'
import { saveBook } from '../../api/books'
import { uploadPhotos } from '../../api/photos'
import { useScanAudio } from '../../hooks/useScanAudio'
import { theme } from '../../styles/theme'

const CONDITIONS = ['New', 'Very Good', 'Good', 'Acceptable', 'Poor'] as const
type Condition = (typeof CONDITIONS)[number]

interface ReviewStepProps {
  lookupResult: BookLookup
  photos: File[]
  savedBookId: string | null
  onSavedBookId: (id: string) => void
  onSaveComplete: () => void
  onCancel: () => void
  skippedPhotography: boolean
}

async function compressPhoto(file: File): Promise<Blob> {
  return new Promise((resolve) => {
    const img = new Image()
    const url = URL.createObjectURL(file)
    img.onload = () => {
      URL.revokeObjectURL(url)
      const maxEdge = 1200
      let w = img.naturalWidth
      let h = img.naturalHeight
      if (w > maxEdge || h > maxEdge) {
        if (w > h) { h = Math.round((h * maxEdge) / w); w = maxEdge }
        else { w = Math.round((w * maxEdge) / h); h = maxEdge }
      }
      const canvas = document.createElement('canvas')
      canvas.width = w
      canvas.height = h
      canvas.getContext('2d')!.drawImage(img, 0, 0, w, h)
      canvas.toBlob((blob) => resolve(blob!), 'image/jpeg', 0.85)
    }
    img.src = url
  })
}

export default function ReviewStep({
  lookupResult,
  photos,
  savedBookId,
  onSavedBookId,
  onSaveComplete,
  onCancel,
  skippedPhotography,
}: ReviewStepProps) {
  const [condition, setCondition] = useState<Condition | null>(null)
  const [reviewMetadata, setReviewMetadata] = useState(!lookupResult.data_complete)
  const [reviewPhotography, setReviewPhotography] = useState(skippedPhotography)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const { playSuccess } = useScanAudio()

  async function handleSave() {
    if (!condition || saving) return
    setSaving(true)
    setError('')

    try {
      let bookId = savedBookId

      if (!bookId) {
        const book = await saveBook({
          ...lookupResult,
          condition,
          data_complete: reviewMetadata ? false : lookupResult.data_complete,
          needs_photo_review: reviewPhotography,
        })
        bookId = book.id
        onSavedBookId(bookId)
      }

      if (photos.length > 0) {
        const blobs = await Promise.all(photos.map(compressPhoto))
        await uploadPhotos(bookId, blobs)
      }

      playSuccess()
      onSaveComplete()
    } catch (e) {
      setError(e instanceof Error ? e.message : 'Save failed — tap Save to retry photo upload')
    } finally {
      setSaving(false)
    }
  }

  return (
    <WorkflowWrapper
      step="review"
      controls={null}
      primaryLabel={saving ? 'Saving…' : 'SAVE'}
      primaryDisabled={!condition || saving}
      onPrimary={handleSave}
      onCancel={onCancel}
    >
      <div
        style={{
          height: '100%',
          overflowY: 'auto',
          padding: '1rem 1.25rem',
          color: theme.colors.text,
        }}
      >
        {/* Cover + metadata */}
        <div
          style={{
            display: 'flex',
            gap: '0.875rem',
            marginBottom: '1.25rem',
            alignItems: 'flex-start',
          }}
        >
          {lookupResult.cover_image_url ? (
            <img
              src={lookupResult.cover_image_url}
              alt="Cover"
              style={{
                width: 64,
                aspectRatio: '2 / 3',
                objectFit: 'cover',
                borderRadius: 4,
                flexShrink: 0,
                border: `1px solid ${theme.colors.border}`,
              }}
            />
          ) : (
            <div
              style={{
                width: 64,
                aspectRatio: '2 / 3',
                background: theme.colors.subtle,
                borderRadius: 4,
                flexShrink: 0,
                border: `1px solid ${theme.colors.border}`,
              }}
            />
          )}
          <div>
            <h2
              style={{
                margin: '0 0 0.25rem',
                fontSize: '1.05rem',
                fontWeight: 700,
                lineHeight: 1.3,
                color: theme.colors.text,
              }}
            >
              {lookupResult.title ?? 'Unknown Title'}
            </h2>
            <p style={{ margin: '0 0 0.1rem', fontSize: '0.9rem', color: theme.colors.muted }}>
              {lookupResult.author ?? '—'}
            </p>
            <p style={{ margin: 0, fontSize: '0.82rem', color: theme.colors.muted }}>
              {[lookupResult.year, lookupResult.publisher].filter(Boolean).join(' · ')}
            </p>
          </div>
        </div>

        {/* Condition selector */}
        <p
          style={{
            margin: '0 0 0.5rem',
            fontSize: '0.75rem',
            color: theme.colors.muted,
            fontWeight: 600,
            textTransform: 'uppercase',
            letterSpacing: '0.05em',
          }}
        >
          Condition
        </p>
        <div style={{ display: 'flex', gap: '0.35rem', marginBottom: '1rem' }}>
          {CONDITIONS.map((c) => (
            <button
              key={c}
              onClick={() => setCondition(c)}
              style={{
                flex: 1,
                padding: '0.45rem 0.1rem',
                fontSize: '0.72rem',
                background: condition === c ? theme.colors.accent : theme.colors.subtle,
                color: condition === c ? '#fff' : theme.colors.text,
                border: condition === c
                  ? `1px solid ${theme.colors.accent}`
                  : `1px solid ${theme.colors.border}`,
                borderRadius: 6,
                cursor: 'pointer',
                fontWeight: condition === c ? 600 : 400,
                fontFamily: theme.font.sans,
                whiteSpace: 'nowrap',
              }}
            >
              {c}
            </button>
          ))}
        </div>

        {/* Review Metadata? (formerly "Mark for Review?") */}
        <label
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: '0.5rem',
            cursor: 'pointer',
            marginBottom: '0.5rem',
            fontSize: '0.875rem',
            color: theme.colors.text,
          }}
        >
          <input
            type="checkbox"
            checked={reviewMetadata}
            onChange={(e) => setReviewMetadata(e.target.checked)}
          />
          Review Metadata?
        </label>

        {/* Review Photography? — auto-checked when skippedPhotography */}
        <label
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: '0.5rem',
            cursor: 'pointer',
            marginBottom: '0.75rem',
            fontSize: '0.875rem',
            color: theme.colors.text,
          }}
        >
          <input
            type="checkbox"
            checked={reviewPhotography}
            onChange={(e) => setReviewPhotography(e.target.checked)}
          />
          Review Photography?
        </label>

        {error && (
          <p style={{ color: theme.colors.danger, fontSize: '0.85rem', margin: '0.5rem 0' }}>
            {error}
          </p>
        )}
      </div>
    </WorkflowWrapper>
  )
}
```

Note: The `uploadPhotos` call is guarded with `if (photos.length > 0)` — this prevents a failed upload attempt when SKIP was used and no photos were taken.

- [ ] **Step 2: Verify TypeScript — expect clean**

```bash
cd frontend && npx tsc --noEmit
```

Expected: **No errors.** All tasks now supply the required props.

- [ ] **Step 3: Run frontend tests**

```bash
cd frontend && npm run test
```

Expected: All tests pass (no existing tests cover these components).

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/workflow/ReviewStep.tsx
git commit -m "feat: ReviewStep — Review Metadata/Photography checkboxes, needs_photo_review on save"
```

---

## Task 11: CLAUDE.md — Design Conventions + CHANGES-07 summary

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Add Design Conventions section**

Add a new `## Design Conventions` section to `CLAUDE.md` (place it after the `## Auth` section and before `## Key Decisions & Gotchas`):

```markdown
## Design Conventions

**Button labels (workflow screens):** Primary action buttons use ALL CAPS — `CAPTURE`, `LOOKUP`, `SAVE`, `SKIP`. Hint text references to buttons use Title Case — "tap Capture", "tap Lookup".

**Primary button height:** All three primary buttons (CAPTURE, LOOKUP, SAVE) render via `WorkflowWrapper` Zone 5 and share `minHeight: 56px`. Never add per-step height overrides.

**Controls bar styling:** Controls bars on workflow screens have `border: 1px solid #E5E5E5`. Interactive controls within (dropdowns, icon buttons) get `background: theme.colors.subtle` so they read as tappable.

**Step indicator zone:** `background: #F0F0F0` (Zone 1 in WorkflowWrapper).

**Secondary button bar:** `background: #F0F0F0` (Zone 6 in WorkflowWrapper). Secondary buttons within use `background: theme.colors.subtle`. Cancel is labelled "Start Over."

**Colors:** All colors reference `theme.colors.*` from `frontend/src/styles/theme.ts`. Do not add hardcoded hex values to components.
```

- [ ] **Step 2: Add CHANGES-07 to Completed Iterations**

Add a `**CHANGES-07**` entry to the `## Completed Iterations` section:

```markdown
**CHANGES-07** — all items implemented:
- FEAT-01: Button case convention documented; hint text "then capture" → "then Capture" fixed
- FEAT-02/03: Step indicator and secondary button bar zones get `#F0F0F0` background
- FEAT-04: "Cancel" renamed "Start Over" across all workflow screens
- FEAT-05/08/09: Controls bar treatment — `1px #E5E5E5` border, interactive controls get `theme.colors.subtle` fill
- FEAT-06: Photo count dropdown extends to 0–5; at 0, progress indicators hidden and button label changes to SKIP
- FEAT-07: SKIP advances to Metadata step; sets `skippedPhotography=true` in page state
- BUG-01: Portrait mask replaced with largest square using `ResizeObserver`
- BUG-02: Hint text "Set number of images, position book, then Capture" moved inside mask pill; duplicate below camera removed
- BUG-03/06/12: All primary buttons share `minHeight: 56px` via WorkflowWrapper
- BUG-04: Landscape barcode mask widened to ~3:1 ratio (`top:32% bottom:32%`)
- BUG-05: Hint text "Align barcode then tap Lookup, or use keyboard" moved inside mask pill; errors surface via WorkflowWrapper hintText only
- BUG-07/08/09: `visualViewport` resize detection in keyboard mode — LOOKUP + secondary buttons stay above keyboard, step indicator + controls anchored top, input centered in remaining space
- BUG-10: 📷 emoji replaced with Lucide `<Camera />` icon
- BUG-11: Field heading removed; placeholder: "Type ISBN-10 or ISBN-13, then tap Lookup" with darker placeholder color via injected `<style>`
- Migration 004: `needs_photo_review BOOLEAN NOT NULL DEFAULT FALSE` added to books table
- ReviewStep: "Mark for Review?" → "Review Metadata?"; new "Review Photography?" checkbox (auto-checked on SKIP); `needs_photo_review` saved to DB on book creation
- Dashboard display of `needs_photo_review` deferred to future CHANGES file
```

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: add Design Conventions section and CHANGES-07 completion summary"
```

---

## Task 12: Run migrations and smoke test

- [ ] **Step 1: Start dev stack**

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
```

- [ ] **Step 2: Run migration**

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml exec api alembic upgrade head
```

Expected output includes: `Running upgrade 003 -> 004`.

- [ ] **Step 3: Smoke test in browser**

Open `https://localhost:3001` (or `https://<mac-ip>:3001` on phone). Verify:
- All three workflow screens load without console errors
- Step indicator zone is `#F0F0F0`
- Secondary button bar is `#F0F0F0`
- "Start Over" appears instead of "Cancel"
- Primary buttons are visibly taller
- Photograph screen: dropdown includes 0; selecting 0 shows SKIP button and hides □ indicators
- Photograph screen: square mask visible and centered
- Metadata camera: landscape mask is wide (~3:1 ratio)
- Metadata keyboard: Camera icon (line art) visible; no label above input; keyboard pushes layout up correctly on iOS

- [ ] **Step 4: Run full backend test suite**

```bash
cd api && .venv/bin/pytest -v
```

Expected: All tests pass.

---

## Self-Review Notes

**Spec coverage check:**
- FEAT-01 ✓ (hint text fix + documented in conventions)
- FEAT-02 ✓ WorkflowWrapper Zone 1
- FEAT-03 ✓ WorkflowWrapper Zone 6
- FEAT-04 ✓ "Start Over" in Zone 6
- FEAT-05 ✓ Task 5 controls bar
- FEAT-06 ✓ Task 5 dropdown 0-5 + SKIP label
- FEAT-07 ✓ Task 5 onSkip + Task 7 state
- FEAT-08 ✓ Task 8 camera controls bar
- FEAT-09 ✓ Task 9 keyboard controls bar
- BUG-01 ✓ Task 6 ResizeObserver square mask
- BUG-02 ✓ Task 5/6 hint inside mask, no WorkflowWrapper hintText
- BUG-03 ✓ Task 4 minHeight 56px
- BUG-04 ✓ Task 8 top:32% bottom:32%
- BUG-05 ✓ Task 8 hint text + hintText = errors only
- BUG-06 ✓ Task 4 minHeight 56px (shared)
- BUG-07/08/09 ✓ Task 9 visualViewport
- BUG-10 ✓ Task 9 Lucide Camera
- BUG-11 ✓ Task 9 placeholder + style tag
- BUG-12 ✓ Task 4 minHeight 56px (shared)
- Migration 004 ✓ Task 1
- ReviewStep checkboxes ✓ Task 10
- needs_photo_review on save ✓ Task 10
- CLAUDE.md conventions ✓ Task 11
